instalar na consola:

npm init -y

npm install express mongoose bcrypt cor multer
(e npm audit fix caso existam vulnerabilidades)

adicionar ("type": "module", depois de "version" no package.json

npm install --save-dev nodemon (opcional)
